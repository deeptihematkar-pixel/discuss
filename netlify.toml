[build]
  base = "frontend"
  command = "yarn install --ignore-engines && yarn build"
  publish = "build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
